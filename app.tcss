#main {
    height: 1fr;
}

Table {
    width: 1fr;
}

SidePanel {
    width: 45;
    border: round white;
    padding: 1;
}

CommunityCards {
    height: 1fr;
    align: center middle;
    border: round white;
}

PlayerHand {
    height: auto;
    align: center middle;
    border: round white;
}

PlayingCard {
    margin-left: 1;
}

PlayingCard:first-of-type {
    margin-left: 0;
}
