# Agent instructions

- Don't verify changes at the end of a turn (e.g. taking screenshots, spinning up `run_test`/headless renders, or re-running the app) unless explicitly asked to. Make the edit and stop there; trust that Edit/Read already confirm the file state.
